#include "main.h"
/**
*mul - multiple two integers
*@a:the first to multiply
*@b:the second to multiply with the first number
*Return: the result of multipication between a and b
*/
int mul(int a, int b)
{
return (a * b);
}
